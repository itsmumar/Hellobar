{{#application-layout viewMode=viewMode isFullscreen=isFullscreen isCallGoal=isCallGoal}}

  {{navigation/side-navigation viewMode=viewMode isFullscreen=isFullscreen goal=goal style=style closeEditor='closeEditor'}}

  <div class="viewport-wrapper">
    <div class="step-wrapper">
      {{outlet}}

      {{navigation/step-navigation
      action='saveSiteElement'
      saveSubmitted=saveSubmitted}}
    </div>

    {{preview/preview-area model=model viewMode=viewMode}}
  </div>

  {{outlet 'interstitial'}}

{{/application-layout}}

