import Ember from 'ember';
import PropertiesForTypeMixin from '../../mixins/properties-for-type-mixin';

export default Ember.Component.extend(PropertiesForTypeMixin, {
  classNames: ['design-step']
});
