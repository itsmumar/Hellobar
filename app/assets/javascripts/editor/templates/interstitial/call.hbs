<header>
  <h5>Talk to Your Visitors</h5>
</header>

<div class="goal-wrapper template-form">
  <i class="large-icon icon-call"></i>

  <h6>What should your Hello Bar say?</h6>
  {{view Ember.TextField value=model.headline placeholder="Talk to us to find out more" maxlength=255}}

  <h6>What should the button say?</h6>
  {{view Ember.TextField value=model.link_text placeholder="Call Now" maxlength=255}}

  <h6>What phone number should be dialed?</h6>
  <div class="input-wrapper">
    {{view Ember.TextField value=controllers.application.phone_number placeholder="(555) 555-555" maxlength=255}}
    <div class="select-wrapper">
      {{view Ember.Select
        content=HB.countryCodes
        value=model.phone_country_code
        optionValuePath='content.code'
        optionLabelPath='content.name'
      }}
    </div>
  </div>

  <div class="step-container">
      {{#link-to "interstitial" replace=true classNames="button cancel-button cancel"}}Cancel{{/link-to}}
    <!--button class="button cancel-button cancel" {{action 'closeEditor'}}>Cancel</button-->
    <button class="button save-editor" {{action 'closeInterstitial'}} {{bind-attr disabled=inputIsInvalid}}>Continue</button>
  </div>
</div>

{{partial "partials/interstitial-footer"}}
