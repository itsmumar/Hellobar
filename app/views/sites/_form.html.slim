= form_for site, url: admin_user_site_path(@user, site), method: :put, html: {class: "hidden add_invoice_form"} do |f|
  div= f.text_area 'invoice_information', placeholder: "Appears below name on invoice and replaces the address from credit card information.", style: "width: 40%;", required: true
  div= f.submit "Submit"
