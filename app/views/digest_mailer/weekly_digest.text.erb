Your performance this week for <%= @site.url %>

<%= format_number(@last_week_statistics.views) %> views last week
<%= format_number(@last_week_statistics.conversions) %> <%= @conversion_header.downcase %> last week
<%= format_number(@week_before_statistics.conversions) %> <%= @conversion_header.downcase %> the week before
<%= format_number(@site_statistics.conversions) %> total <%= @conversion_header.downcase %> ever

<%= @conversion_header %>

<% @sorted_elements.each_with_index do |site_element, index| %>
<%= index + 1 %>) <%= strip_tags(site_element.headline || '').html_safe %>
  * Views: <%= site_element.statistics.within(@last_week).views %>
  * <%= @conversion_header %>: <%= site_element.statistics.within(@last_week).conversions %> (<%= formatted_percent(site_element.statistics.within(@last_week).conversion_percent, false) %>)
<% end %>

Get more conversions by visiting <%= site_site_elements_url(@site) %>

<% if @site.free? %>
  Get 4x more <%= @conversion_header.downcase %> by upgrading to Pro!
  * Brand it as your bar!  No Hello Bar logo in your bar.  It's all yours!
  * Remove Ads! Increase your results by removing ads.
  * Targeted messages! Show different messages to different users.
  * Better stats! See tips to improve your marketing.

  Get all the Pro features for $15.  Visit <%= edit_site_url(@site) %>
<% end %>


You are receiving weekly updates. You can turn off or change your settings by visiting your email settings page at <%= profile_url %>
