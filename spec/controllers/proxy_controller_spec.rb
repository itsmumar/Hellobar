require 'spec_helper'
describe ProxyController do
  #Delete this example and add some real ones
  it "should use ProxyController" do
    controller.should be_an_instance_of(ProxyController)
  end
end
